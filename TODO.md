# TODO

## 2025-05-08

I've created `smartmooring.py` and `location_pd.py` which are standalone parsers for the
SMD and LOC files, respectively.

- [ ] Integrate `smartmooring.py` and `location_pd.py` into `sd_file_parser.py`
- [ ] Rewrite `sd_file_parser.py` to work more like `smartmooring.py` and `location_pd.py`
